#!/bin/sh
# Activates the Pacific Linux Plymouth theme and rebuilds the initramfs so it
# takes effect in the image live-build assembles. Runs inside the chroot
# during `lb build`.

set -e

plymouth-set-default-theme pacific-linux --rebuild-initrd
